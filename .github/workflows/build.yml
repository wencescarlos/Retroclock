name: Build Firmware

permissions:
  contents: write

on:
  push:
    branches: [ main, develop ]
    tags: [ 'v*' ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.9'

    - name: Install system dependencies
      run: sudo apt-get update -qq && sudo apt-get install -y libusb-1.0-0-dev udev

    - name: Cache PlatformIO
      uses: actions/cache@v5
      with:
        path: ~/.platformio
        key: platformio-${{ hashFiles('platformio.ini') }}

    - name: Install PlatformIO
      run: |
        python -m pip install --upgrade pip
        pip install platformio==6.1.16

    - name: Build firmware
      run: platformio run -e CYD_RetroClock

    - name: Upload firmware artifacts
      uses: actions/upload-artifact@v7
      with:
        name: firmware
        path: .pio/build/CYD_RetroClock/
        retention-days: 30

    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: |
          .pio/build/CYD_RetroClock/firmware.bin
          .pio/build/CYD_RetroClock/bootloader.bin
          .pio/build/CYD_RetroClock/partitions.bin
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
